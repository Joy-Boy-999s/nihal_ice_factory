// Stub: class-transformer/storage is Node.js-only metadata storage.
// The frontend never runs validators — this is a safe no-op.
export default {};
export const defaultMetadataStorage = {};
export const getMetadataStorage = () => ({});
